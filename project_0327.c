/*
#include <stdio.h>
int main(void)
{

    # 숫자 입력 받기
    int a;
    printf("숫자 하나를 입력하세요: ");
    scanf("%d", &a);
    
    printf("입력한 숫자는 %d입니다. \n", a);
    
    # ASCII
    printf("%d \n", 'A');
    printf("%c \n", 'A');
    
    printf("%d \n", 3/2);
    
    # 형 변환
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%f \n", (double)a/(double)b);
    
    # If
    int a = 10, b = 10;
    if (a == b) {
        printf("a와 b는 같다 \n");
    }
    else {
        printf("a와 b는 다르다 \n");
    }
    
    int a = 10, b = 10;
    if (a > b) {
        printf("a는 b보다 크다 \n");
    }
    else if (a == b) {
        printf("a와 b는 같다 \n");
    }
    else {
        printf("a와 b는 다르다 \n");
    }
    
    # While
    int i = 0;
    while (++i<10) {
        printf("%d \n", i);
    }
    
    int i = 0;
    while (i++<10) {
        printf("%d \n", i);
    }
    
    
    
    # For
    for (int i = 0; i<10; i++) {
        printf("%d \n", i);
    }
    
    # 변수를 밖에서 지정하는 것도 가능하긴 함.
    int i = 0;
    for (; i<10; i++) {
        printf("%d \n", i);
    }
    
    return 0;
}
*/
