#include <stdio.h>

/*
int add(int x, int y) {
    return x+y;
}

void menu(void) {
    printf("1. Add \n");
}
int main(void) {
    int a, b;
    menu();
    a = 10, b = 20;
    printf("Sum of %d and %d is %d \n", a, b, add(a,b));
    return 0;
}

void swap(int, int);

int main(void){
    int a = 10, b = 20;
    swap(a, b);
    printf("%d %d \n", a, b);
    
    return 0;
}

void swap(int x, int y){    //call by value, call by reference
    int tmp;
    tmp = x;
    x = y;
    y = tmp;
}

void recursive(int);

int main(void){
    recursive(10);
    return 0;
}
void recursive(int n){
    printf("recursive call! \n");
    if (n==1) return;
    recursive(n-1);
}


#include<stdlib.h>

int main(void){
    int i;
    i = rand();
    printf("%d \n", i);
    return 0;
}

#include<stdlib.h>
#include<time.h>

int main(void){
    srand((unsigned)time(NULL));
    int i;
    i = rand()%45+1;
    printf("%d \n", i);
    return 0;
}
 */
